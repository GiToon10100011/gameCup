// Playwright E2E 풀 플로우 — 검색 → 후보 등록 → 토너먼트 진행 → 결과까지 한 번에 검증.
// Phase 4(테스트 정비) 단계에서 실제 시나리오로 채운다. 현재는 fixme로 스킵.

import { test } from "@playwright/test";

// IT-01~05 통합 시나리오는 Sprint 3~4에서 활성화 예정
test.fixme("IT-01~05 풀 플로우: 검색 → 후보 → 토너먼트 → 결과 (Phase 4에서 구현)", async ({ page }) => {
  // 1) 메인 화면 진입
  await page.goto("/");

  // TODO Phase 4:
  // 2) 검색 → 결과 드롭다운 노출 (IT-01)
  // 3) 후보 등록 → 후보 목록에 표시 (IT-02)
  // 4) 토너먼트 시작 → 1:1 대결 화면 (IT-03)
  // 5) 게임 선택 → 다음 페어/라운드 진행 (IT-04)
  // 6) 우승자 결과 화면 + 새 토너먼트 시작 (IT-05)
});
